import { generateRandomStealthMetaAddress } from '../../..';
function setupTestStealthKeys(schemeId) {
    const { spendingPrivateKey, spendingPublicKey, stealthMetaAddressURI, viewingPrivateKey, viewingPublicKey } = generateRandomStealthMetaAddress();
    return {
        spendingPublicKey,
        spendingPrivateKey,
        viewingPublicKey,
        viewingPrivateKey,
        stealthMetaAddressURI
    };
}
export default setupTestStealthKeys;
